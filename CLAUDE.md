# CLAUDE.md — このリポジトリの作業ルール

このファイルはClaude Codeが作業を開始する際に自動的に読み込む指示書です。
必ずすべて読んでから作業を開始してください。

---

## デプロイの仕組み

- `main` ブランチへのプッシュ → GitHub Actions が自動起動
- `./site/` ディレクトリの内容を WPX サーバー（buymo.me）へ FTP デプロイ
- **差分同期**（SamKirkland/FTP-Deploy-Action）: git で変更されたファイルだけを再アップロードする

## 絶対に守るルール

### 1. FTPサーバーを直接編集してはならない
FTPで直接ファイルを編集・削除しても、次に git からデプロイが走ると **git の内容で上書きされる**。
すべての変更は必ず git にコミットしてから行うこと。

### 2. ファイルを変更する前に必ず最新内容を読む
GitHubのMCP APIで `get_file_contents` を使い、最新のファイル内容とSHAを確認してから編集する。
古いSHAでコミットしようとするとエラーになる。

### 3. 画像やファイルを追加するときはgit経由で
FTPの差分同期の都合で、gitに存在しないファイルはFTPサーバーにも存在しない（または過去のデプロイ時から変わらない）。
新しい画像等は必ずリポジトリにコミットすること。

---

## サイトの重要な仕様（変更禁止・削除禁止リスト）

### 電話番号の表示
- **050-1784-2929** はサイトに表示しない（ヘッダー・フッター・追従CTA・会社情報すべて）
- この番号をHTMLに追加・復元しないこと
- フォームの入力フィールド（`<input type="tel">`）はユーザー入力欄なので問題なし

### 表示してよい連絡先
- メールアドレス: `info@aisjaltd.com`（フッターに表示済み）

---

## サイト構成

```
site/
  buymo.html          # トップページ（メインのLP）
  assets/
    css/buymo.css     # メインCSS（バージョンクエリ ?v=N でキャッシュ制御）
    js/               # JavaScript各種
    img/buymo/        # 画像ファイル
  area/               # エリア別ページ
  genre/              # ジャンル別ページ
```

## よくあるトラブルと対処法

| 症状 | 原因 | 対処 |
|------|------|------|
| CSSが古い表示のまま | ブラウザキャッシュ | `buymo.css?v=N` の N を +1 してコミット |
| 画像が表示されない | FTPに未アップロード | SVGはHTMLにインラインで埋め込む |
| 削除したはずの内容が復活 | FTP直接編集がgit pushで上書き | gitで削除してコミットする |
| `reveal` クラスの要素が見えない | JSのIntersectionObserverが未発火 | ヒーローなど初期表示要素には `reveal` クラスを付けない |

---

## コミット・プッシュの手順

このリポジトリでは `git clone` が失敗する環境が多い。
代わりに **GitHub MCP API** を使う:

1. `mcp__github__get_file_contents` で最新ファイル内容とSHAを取得
2. 内容を編集
3. `mcp__github__create_or_update_file` でコミット（`sha` パラメータ必須）
4. ブランチは `main`

---

最終更新: 2026-07-26
